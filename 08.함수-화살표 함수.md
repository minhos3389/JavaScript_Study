## 화살표 함수

```JS
const add = (a, b) => {
  return a + b;
};

const sum = add(1, 2);
console.log(sum); // 3 출력

const hello = (name) => {
  console.log(`Hello, ${name}!`);
};

hello('mitku'); // Hello, mitku! 출력

```
